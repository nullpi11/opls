# OP·RIFT — Netlify 설정

[build]
  publish   = "."
  functions = "netlify/functions"
  command   = ""

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from   = "/api/*"
  to     = "/.netlify/functions/riot/api/:splat"
  status = 200
